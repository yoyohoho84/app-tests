import React from "react";

import {
  Panel,
  PanelHeader,
  Button,
  Div,
  Group,
  Header,
  Cell,
} from "@vkontakte/vkui";

const Home = ({ id, go, fetchedUser }) => {
  return (
    <Panel id={id}>
      <div>123</div>
    </Panel>
  );
};

export { Home };
